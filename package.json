{
  "devDependencies": {
    "@types/jest": "^29.2.0",
    "@types/node": "^18.11.7",
    "prisma": "^4.5.0",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@prisma/client": "^4.5.0",
    "dotenv": "^16.0.3",
    "grammy": "^1.11.2"
  },
  "scripts": {
    "build": "rm -rf build/ && tsc --build",
    "clean": "rm -rf build/ && tsc --build --clean",
    "test": "jest",
    "dev": "ts-node src/bot.ts"
  }
}
